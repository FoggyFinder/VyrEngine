<Window x:Class="VyrEditor.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VyrEditor.WPF"
        xmlns:vm="clr-namespace:VyrViewModel;assembly=VyrViewModel"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource VMLocator}, Path=Main}"
        Title="MainWindow" Height="350" Width="525">
    <!--    <Window.DataContext>
        <local:TestVM x:Name="TestVM" />
    </Window.DataContext> Command="{Binding TestCommand}"
   
           -->
    <Grid>
        <Button Width="65" Height="65" Command="{Binding InitializeEngineCommand}"   />
    </Grid>
</Window>
